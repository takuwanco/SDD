# 設計計画工程

このファイルは、仕様駆動開発の**設計計画工程**で作成される設計計画書です。

参照: [原則決定工程](./01-principle-definition.md) | [要件定義](./02-planning-requirement.md)

## 技術スタック選択肢

{% if technology_options is string %}
{{ technology_options }}
{% elif technology_options is mapping %}
{% for category, options in technology_options.items() %}
### {{ category }}

{% if options is string %}
{{ options }}
{% elif options is sequence %}
{% for option in options %}
- **{{ option.name if option is mapping else option }}**
{% if option is mapping and option.pros is defined %}
  - 長所: {{ option.pros | join(', ') if option.pros is sequence else option.pros }}
  - 短所: {{ option.cons | join(', ') if option.cons is defined and option.cons is sequence else option.cons if option.cons is defined else '（記載なし）' }}
  - 選択理由: {{ option.reason if option.reason is defined else '（記載なし）' }}
{% endif %}
{% endfor %}
{% endif %}

{% endfor %}
{% else %}
{% for option in technology_options %}
- {{ option }}
{% endfor %}
{% endif %}

## アーキテクチャ

{% if architecture is string %}
{{ architecture }}
{% elif architecture is mapping %}
### システム構成

{{ architecture.overview if architecture.overview is defined else "（記載なし）" }}

### アーキテクチャパターン

{{ architecture.pattern if architecture.pattern is defined else "（記載なし）" }}

### データフロー

{{ architecture.data_flow if architecture.data_flow is defined else "（記載なし）" }}
{% else %}
{{ architecture }}
{% endif %}

## 設計上の制約

{% if design_constraints is defined %}
{% for constraint in design_constraints %}
- {{ constraint }}
{% endfor %}
{% else %}
設計上の特別な制約はありません。
{% endif %}

## 設計決定記録（ADR: Architecture Decision Records）

{% if design_decisions is string %}
{{ design_decisions }}
{% elif design_decisions is mapping %}
{% for decision_id, decision in design_decisions.items() %}
### {{ decision_id }}: {{ decision.title if decision.title is defined else "（タイトルなし）" }}

**日付**: {{ decision.date if decision.date is defined else generation_date }}

**背景**: {{ decision.context if decision.context is defined else "（記載なし）" }}

**決定内容**: {{ decision.decision if decision.decision is defined else "（記載なし）" }}

**理由**: {{ decision.rationale if decision.rationale is defined else "（記載なし）" }}

**影響**: {{ decision.consequences if decision.consequences is defined else "（記載なし）" }}

---

{% endfor %}
{% else %}
設計決定記録は随時追加されます。
{% endif %}

---

**変更履歴**

| 日付 | 変更内容 | 変更者 |
|------|----------|--------|
| {{ generation_date }} | 初版作成 | Spec AIライター |

---

*このドキュメントは Spec AIライター によって自動生成されました*
*プロジェクト名: {{ project_name }}*
